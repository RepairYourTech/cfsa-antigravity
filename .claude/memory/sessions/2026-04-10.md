# Session: 2026-04-10

## Context
Convert `.claude` from a runtime wrapper over `.agent` into a standalone Claude Code runtime.

## Work Completed
- Replaced `.claude/skill-library` symlink with a local copied directory.
- Added `.claude/kit-sync.md` and `.claude/progress/` ownership for Claude runtime state.
- Rewrote Claude instructions, workflow skills, session continuity protocols, and references away from `.agent/*` runtime paths.
- Removed workflow parity-source blocks from Claude workflow files.
- Updated `scripts/build-template.sh` to copy `.claude` as an owned tree, clean `.claude/progress/`, and generate `.claude/kit-sync.md` in the template.
- Updated `scripts/check-template-integrity.sh` to validate standalone Claude invariants instead of `.agent` parity blocks.
- Updated key docs (`.claude/README.md`, `README.md`, `CLAUDE.md`, `docs/kit-architecture.md`) to reflect standalone Claude ownership.
- Verified with `npm run build`, `./scripts/check-template-integrity.sh`, and a direct `.agent/` reference scan under `.claude`.

## Session End
**Status**: complete
**Next Steps**: Review the diff, decide whether to also rewrite remaining top-level docs that still describe the `.agent` runtime as the canonical architecture, and then commit if satisfied.
**Checkpoint**: Standalone `.claude` migration is implemented and integrity checks are green.
