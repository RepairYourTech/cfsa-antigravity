Run architecture decomposition.

Use skill: `decompose-architecture`.
Reference: `.claude/skills/workflows/workflow-decompose-architecture.md`.

If arguments are provided, apply them as workflow context: $ARGUMENTS
