Run operational workspace setup.

Use skill: `setup-workspace`.
Reference: `.claude/skills/workflows/workflow-setup-workspace.md`.

If arguments are provided, apply them as workflow context: $ARGUMENTS
