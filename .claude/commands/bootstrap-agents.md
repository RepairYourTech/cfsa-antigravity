Run bootstrap-agents workflow.

Use skill: `bootstrap-agents`.
Reference: `.claude/skills/workflows/workflow-bootstrap-agents.md`.

If arguments are provided, apply them as workflow context: $ARGUMENTS
