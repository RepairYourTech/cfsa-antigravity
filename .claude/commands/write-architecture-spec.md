Run IA spec writing.

Use skill: `write-architecture-spec`.
Reference: `.claude/skills/workflows/workflow-write-architecture-spec.md`.

If arguments are provided, apply them as workflow context: $ARGUMENTS
