Run single-slice implementation.

Use skill: `implement-slice`.
Reference: `.claude/skills/workflows/workflow-implement-slice.md`.

If arguments are provided, apply them as workflow context: $ARGUMENTS
