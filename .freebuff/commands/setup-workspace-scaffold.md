Run setup-workspace-scaffold workflow.

Use skill: `setup-workspace-scaffold`.
Reference: `.freebuff/skills/setup-workspace-scaffold/SKILL.md`.

If arguments are provided, apply them as workflow context: $ARGUMENTS
