---
"cfsa-antigravity": minor
---

Add a `--agent freebuff` target to `cfsa-antigravity init`, shipping a standalone `.freebuff/` CFSA runtime (skills, rules, instructions, commands, and skill library) derived from the Claude Code runtime.
