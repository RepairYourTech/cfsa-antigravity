---
"cfsa-antigravity": patch
---

Add Claude memory artifacts (`patterns.md`, `decisions.md`, `blockers.md`, sessions README) and enforce a native-memory bridge so CFSA memory capture dual-writes relevant entries to Claude native memory types.
