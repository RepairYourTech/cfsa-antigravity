---
"cfsa-antigravity": patch
---

Add Claude slash-command shims for all workflow skills (including `/audit-ambiguity`, shard workflows, and utility workflows) so every shipped Claude workflow is directly invokable via `.claude/commands/`.
