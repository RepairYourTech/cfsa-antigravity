---
"cfsa-antigravity": minor
---

Add a `--agent zcode` target to `cfsa-antigravity init`, shipping a `.zcode/` workspace runtime (slash commands, MCP config, and graphify hooks) that reuses the shared `.agents/` skill library.
