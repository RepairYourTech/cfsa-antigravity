---
"cfsa-antigravity": minor
---

Add Claude Code workflow parity for the full CFSA pipeline and utility workflows, including setup and validation flows. Introduce integrity guardrails that enforce `.agent`↔`.claude` workflow filename parity and required source-reference blocks in CI checks.
