# Deep Dives

Deep dive documents for complex features. Created during `/write-architecture-spec` when a feature is too complex for the main IA shard.

Deep dives are referenced from their parent shard and provide exhaustive detail on specific subsystems, algorithms, or interaction patterns that would make the parent shard unwieldy.
