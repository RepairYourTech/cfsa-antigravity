# Phase Plans

Phase plans created by `/plan-phase`. Each phase defines a set of vertical slices with acceptance criteria, ordered by dependency and risk.

## Naming Convention

Phase files are numbered sequentially: `phase-1.md`, `phase-2.md`, etc.

## Completion Requirement

Each phase must pass `/validate-phase` before the next phase begins.
